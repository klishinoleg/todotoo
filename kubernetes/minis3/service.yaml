apiVersion: v1
kind: Service
metadata:
  name: minis3
spec:
  type: ClusterIP
  selector:
    app: minis3
  ports:
    - name: s3
      port: 9000
      targetPort: 9000
    - name: console
      port: 9001
      targetPort: 9001

